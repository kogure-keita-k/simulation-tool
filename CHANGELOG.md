# CHANGELOG ― DPS 試算アプリ

バージョンは改修規模で自動更新（大＝新画面/算出方法の追加削除、中＝算出方法・実装済み機能の修正、小＝見た目）。
※ 出力資料(PPTX等)関連は「機能追加」以外はすべて軽微（パッチ）扱い。
※ 削減・回収の中核式（calc_simulation）は全期間を通じて**無改変**。

---

## v1.13.4（追加：各種設定 画像出力①の取込でも年月の返りを警告）
- 各種設定『画像出力』①グラフのExcel取込で、**取込元データに年月の返り／重複があった場合に警告**を表示（自動連番ONで補正済みでも「元データに返りがあった」旨を①エディタ横に明示）。
- ※ ホーム画像生成の動作確認：サンプル／DPSテンプレ／トクラス取込のいずれでも①②③が生成されることを実機（v1.13.4）で確認済み。落ちる場合は Streamlit の再起動（v1.13.2の修正反映）をご確認ください。

## v1.13.3（復活：取込時に年月の返りを警告）
- 自動連番（v1.12.3・既定ON）が取込時に月を補正してしまい、**年月の返り警告が出なくなっていた**問題に対応。
  - 取込時に **補正前の"元データ"の年月の返り／重複を判定**し、
    - 自動連番ON：`⚠️ 取込データに年月の返りがありました：… → 先頭月から連番に自動補正しました（原本のまま取り込むならトグルをOFFで再取込）` と**警告表示**。
    - 自動連番OFF：`⚠️ 注意！！！ 年月の返り／重複があります：… ご確認ください` と**赤エラー表示**。
  - 対象：ホームの検針票インポート（`st.warning`/`st.error`）、各種設定『画像出力』①グラフ取込（取込メッセージに付記）。
- 既存の編集テーブル・画像生成箇所の返り警告（v1.12.1/1.12.2）は従来どおり。

## v1.13.2（バグ修正：12ヶ月以外の取込でシミュ/画像生成が落ちる）
- `sanitize_columns` が、**12行以外の表を取り込み、かつ必須列が欠けている**ときに `ValueError: Length of values (12) does not match length of index (N)` で落ちる不具合を修正（既定値を12個固定で埋めていたのを行数に合わせるよう修正）。
  - この関数は `calc_simulation` からも呼ばれるため、該当ファイル（例：効果試算Excelを検針票として取り込む等）を入れると**試算が失敗→結果が出ない→ホームの画像生成もできない**状態になっていた。
  - 修正後は行数に合わせて既定値を展開し、落ちずに取り込める。
- 検証：トクラス（39行相当）・DPSテンプレ(5)（12行）とも sanitize→画像①生成まで通ることを確認。ブラウザ実機でホームの画像生成（①②③）が復帰することも確認。

## v1.13.1（仕上げ：空調機リストをタイトル名で引用・列位置非依存）
- 各種設定『画像出力』②制御可否リストの取込（`admin_import_units_list`）を、**見出し（タイトル）名で引用**する方式に強化（中断していた分を完了）。
  - 見出し語を拡張：メーカー＝製造元/製造者/ブランド、型式＝機種/形式/品番、制御可否＝発停/可否、系統名＝機器ID/名称/号機/ラベル、設置場所＝設置/位置/エリア/階 等。
  - 見出しの半角/全角スペースを除去して照合。**どの列に書かれていても・列順が違っても引用可能**。
- 検証：トクラス／DPSテンプレ(5)（従来どおり）に加え、別名見出し＋列順シャッフルの合成ファイルでも正しく取込。

## v1.13.0（対応：2種類のExcel形式どちらでも取込可能に）
- インポートExcelを **①効果試算形式（トクラス／ゼンケン等：『計算』『入力』『室外機情報』シート）** と **②DPSテンプレ形式（電力データ入力／室外機リスト）** の**どちらでも取込可能**に。
  - 各種設定『画像出力』①グラフ取込（`admin_import_input_graph`）を刷新：
    - シートは **『計算』優先→『入力』→その他**（効果試算Excelは「計算」シートに全数値があるためそこを参照）。
    - 月セルを **日付だけでなく文字列（2025/04・2025年4月・202504）でも認識**（DPSテンプレの文字列月に対応）。
    - グリッド型では **削減量(kWh)** も取り込み（あれば①グラフの削減系列へ反映）。最大需要は予測値でなく実測/採用値を採用。
  - 空調機（室外機）リストの取込は **ヘッダー（タイトル）名で引用**する方式を強化（`_unit_col`／`admin_import_units_list` の見出し語を大幅拡張：機種・製造元・発停可否・系統・号機 等）。列の位置に依存せず、どの列に書かれていても引用可能。
  - 取込後は既存の年月自動連番（v1.12.3）で月ヘッダのズレ（例：途中で5月に戻る）も補正。
- 検証：トクラス（計算シート）・DPSテンプレ(5) の両方で、①グラフ／②制御可否リスト／ホーム機体入力の全経路が正しく取込。

## v1.12.3（追加：取込時に年月を先頭月から自動連番）
- インポート時、**先頭に記載された年月を起点に、以降の月を自動で連番**に整えて取り込む機能を追加（例：先頭『2025年5月』→ 6月/7月/…/翌4月）。12月→翌1月は年を繰り上げ。
  - 対象：ホームの検針票インポート、各種設定『画像出力』①グラフのExcel取込。
  - **トグルで切替**（既定ON＝自動連番）。OFFにすると取り込んだ年月をそのまま使用（あえてその年月にしたい場合）。
  - 先頭の表記スタイル（2025年5月／2025/05／月のみ）を踏襲。datetime文字列は `YYYY/MM` に自動整形。先頭が解釈不能なら元のまま。
  - 連番ONのときは年月が常に正順になるため、v1.12.1の『返り』警告は自然に出なくなります。

## v1.12.1（追加：年月の返り（前戻り）チェック）
- 推移グラフの元になる年月列で、**先頭から見て年月が前に戻る／同一年月が重複する**箇所を検出し、赤いエラーで警告するように変更。
  - 対象：ホームの『手動データ編集テーブル（検針票）』と、各種設定『画像出力』①グラフの月入力。
  - **12月→1月の年跨ぎは正常**として許容。`2025年12月/1月/2月/…/5月/1月` のような逆行のみ警告（例：「7行目『1月』：月が前に戻っています」）。
  - 判定は月のみ表記・YYYY/MM・YYYY年M月・明示年の逆行に対応（`check_month_regression`）。算出ロジックには影響しません（警告のみ）。

## v1.12.0（追加：時間帯別単価(TOU)の反映オプション ※既存算出は不変）
- **時間帯別単価(TOU)を反映する任意オプションを追加**（サイドバー『分析オプション』にトグル、既定OFF）。
  - 削減kWhを、その月に記載のある時間帯別単価（夏季ピーク/平日昼間/夜間休日）で評価。**時間帯別単価が無い月・OFF時は従来の電力量単価に一致**するため、**既存の試算結果・保存案件・提案書の数字は一切変わりません**（追加専用）。
  - 反映は**月ごとに自動**（単価のある月だけ／通常は夏だけ）。手動で月を選ぶ必要なし。
  - **配分ポリシーを選択可**：平均採用（中立・既定）／ピーク採用（上振れ）／保守採用（控えめ）。同一月に複数バンドがある場合の評価方法。1バンドのみの月はその単価を使用。
  - 時間帯別単価を検出したら、その旨をサイドバーに通知（トグルは常設でいつでも切替→再試算）。
- 整合性：TOU-ON時は『導入後料金＝現状料金−基本料金削減−電力量削減』で導出し、**料金比較グラフと削減額が常に一致**。
- **提案書PPTX・稟議Excelに注記を出力**：TOU反映時、前提注記に「時間帯別単価(TOU)を反映：…『（ポリシー名）』で評価」を表示（PPTXは脚注、Excelは前提シートに追記／数式は無改変）。
- 検証：OFF＝旧データ（touキー無し）＝ビット一致、ON＝TOU対象月のみ増加、料金差＝削減額の一致をヘッドレスで確認。
- ※ `calc_simulation` の従来経路（OFF分岐）は無改変。

## v1.11.4（バグ修正：数量が空だと「cannot convert float NaN to integer」）
- ハードウェア関連費・見積機材などの金額集計で、テーブルの**空行（数量/単価がNaN）**があると `ValueError: cannot convert float NaN to integer` で落ちる不具合を修正。
  - 原因：`int(r.get("数量", 0) or 0)` は NaN が真値のため NaN を返し、`int(NaN)` で例外化していた。
  - 対処：NaN/None/空/非数を安全に0へ丸める共通ヘルパー `_num0()` を追加し、数量・単価の集計/表示（HW費合計・概算HW費・見積機材・室外機1台あたり数量・温湿度センサー数・換算表記）を全て置換。
- 中核式（calc_simulation）は無改変。

## v1.11.3（本業商品設定の入力項目を撤去）
- 本業売上換算スライドの削除に伴い、シミュレーション画面の投資設定内「本業商品設定」（主力商品単価・粗利益率）の**入力項目を撤去**。
- 値は各種設定のマスタ（item_price / item_margin）をそのまま使用するため、Excel等に残る本業換算表記は従来どおり動作（各種設定での編集は引き続き可能）。

## v1.11.2（バグ修正：インポート後にテーブル行を削除するとエラー）
- 機体入力・ハードウェア関連・見積の機材テーブルで、**行を手動削除するとエラー**（特にインポート後）になる不具合を修正。
  - 原因：これらの `data_editor` に固定 key を付けたまま、編集結果を毎回 `session_state` へ書き戻していたため、Streamlit が保持する「削除/追加」の編集状態が新しいデータに**二重適用**され、削除行のインデックスがずれて例外化していた。
  - 対処：3つのエディタの key を外し、**入力データ駆動**（毎回の入力DataFrameをそのまま表示）に変更。削除・追加・インポート差し替えが正しく反映される。
- 中核式（calc_simulation）は無改変。

## v1.11.1（バグ修正：機体入力エディタの型不整合）
- 機体入力テーブルで「機器ID/場所」等の列が空だと pandas が float 型と推定し、`StreamlitAPIException: column type text ... not compatible ... FLOAT` が発生していた不具合を修正。エディタ生成前に文字列列を object 型へ明示キャスト（稼働係数は数値、制御可否は○/×へ正規化）。インポート／スナップショット復元後に空列になっても再発しないよう恒久対処。
- 中核式（calc_simulation）は無改変。

## v1.11.0（データ管理・スナップショット受け渡し・案件比較）
- **各種設定に「データ管理（バックアップ）」タブを追加**：
  - `dps_app_data.json` を **丸ごとバックアップDL**（保存案件/スナップショット/企業データ件数を表示）。
  - バックアップ .json を **アップロードして完全復元**（確認チェック付き・上書き注意表示）。
- **スナップショット単体の受け渡し**：入力条件スナップショットを **1件だけ .json で書き出し／取り込み**。JSON全体を共有せず、案件の入力条件だけを別PC・他担当へ渡せる（同名は上書き）。
- **結果一覧に「並べて比較」を追加**：保存案件を2〜3件選び、業態・空調台数・初期費用・年間削減量・年間削減額・回収年数を横並び比較。
  - 比較で初期費用を出すため、保存案件レコードに `invest`（初期費用）を追加保存（旧案件は「—（旧保存）」表示）。
- 中核式（calc_simulation）は無改変。

## v1.10.0（出力ファイル名規則・結果一覧の検索・入力スナップショット）
- **エクスポート ファイル名を統一**：`エクスポート年月日_企業名_種別.拡張子`（例 `20260710_ゼンケン_提案書.pptx`）。敬称（御中/様/株式会社）・空白・使用不可記号を自動除去。提案書PPTX／試算Excel／削減レポートPDF・Excel／請求書PDF・Excel／結果画面のPNG（グラフ・制御可否リスト・サマリカード）に適用。
- **結果一覧に検索を追加**：企業名・業態でしぼり込み（該当件数表示）。削除は検索結果から選んでも元データの正しい位置に反映。
- **入力条件のスナップショット保存＆呼び出し**：シミュレーション画面に「💾 入力条件のスナップショット」を追加。顧客名・業態・検針票12ヶ月・台数（概算/手動）・概算前提・初期費用設定・分析オプションを1件として保存し、後日・別PCで呼び出して同じ条件を再現（同名は上書き／削除可）。データは `dps_app_data.json` の `input_snapshots` に保存。
  - 復元を確実にするため、顧客名・業態・手動台数/材料費/工事費/エリアの各入力欄をキー付きウィジェットに統一（挙動は同じ）。
- 中核式（calc_simulation）は無改変。

## v1.9.0（概算の前提を案件ごとに手動調整）
- 概算モードON時に **「🔧 概算の前提（この案件用に調整）」** パネルを追加。従来は全自動だった前提値を、案件単位で手入力・上書き可能に（既定はマスタ値を反映）。
  - **標準室外機の定格出力（kW/台）**：概算台数＝空調ピーク負荷÷この定格。定格を変えると推定台数が即時に変わる。
  - **概算 制御比率（％）**：制御台数の初期値。
  - **空調ピーク比率の上書き（％／0＝業態マスタ）**。
  - **材料費・工事費（円/台）／ハードウェア固定費・単価（円）**：初期費用の各概算メトリクスへ即反映。
- `estimate_units_from_meter()` に `overrides` 引数を追加（後方互換：未指定はマスタ既定）。台数手入力（総台数・制御台数）は従来どおり。
- 中核式（calc_simulation）は無改変。

## v1.8.0（制御可否リストの列選択・画像出力のExcel取込）
- **制御可否リスト画像に列選択を追加**：系統名／設置場所／メーカー／型式／制御可否から不要な列を外して生成可（シミュレーション画面の「スライド貼付用 画像出力」＋各種設定「画像出力」の両方）。列を外すと残り列の幅を自動再配分。
- **各種設定「画像出力」②の制御可否を〇×表記に変更**（従来のチェックボックス→〇/×セレクト。シミュレーション画面と統一）。
- **各種設定「画像出力」にExcel取込を追加**：試算Excel（『入力』シート→①グラフ／『室外機情報』シート→②制御可否リスト）を取り込んで各表へ自動反映。横並びグリッド／フラット表のどちらのレイアウトも自動判定。※Excelは形式の一例で値は焼き込まず、アップロード内容のみ反映。
- 中核式（calc_simulation）は無改変。

## v1.7.4（バグ修正）
- 画像出力の①グラフで、使用量・削減が全て0のときに発生していた **division by zero** を修正（左軸上限に下限ガード）。

## v1.7.x（概算の資料化・アイコン）
- **v1.7.2** 概算説明ページのアイコンを緑ライン画のプレースホルダで生成（icon_gaisan1/2/3.png。正式版は同名で上書き差替）。
- **v1.7.1** 概算説明ページを縦3カード→**横並び3カード**（P.3風）に。アイコンは仮置き自動差替対応。
- **v1.7.0** 概算モード時、提案書に**「概算です」の注釈**と**概算の算出ロジック説明ページ**を分析方法の直後へ自動挿入。数値ページ下部の注釈も概算明示に切替。

## v1.6.0（概算3シナリオ）
- 概算モードの結果に**3シナリオ比較（ベスト／標準／保守）**を追加。制御可能比率(100/90/80%)・機材費・1装置あたり制御台数(HW費のみ)・台数推定kWを変えて比較。管理画面マスタで調整可。

## v1.5.0（削減レポ 眼鏡モード）
- 削減レポに**エクセル眼鏡モード（営業日数補正）**トグルを追加。使用電力量の削減効果を「対象年を基準年の営業日条件へ換算」して算出（眼鏡の差分計算シートと一致を検証）。通常モードは単純差のまま。

## v1.4.x（企業データ蓄積・番号修正）
- **v1.4.1** 会社情報の TEL/FAX を修正（03-6402-2650 / 2651）。保存済みjsonも訂正。
- **v1.4.0** **企業別 電力実績データストア**を追加。シミュ保存時・削減レポ取込時に月次を企業へ自動累積（表記ゆれは正規化キーで統一）。削減レポで「企業の蓄積データから呼び出す」で再利用可。請求書用に燃料費調整額・再エネ賦課金も保存。

## v1.3.x（概算モード・削減レポ改善・UI）
- **v1.3.7** 削減レポの基準年指定を復活＋**年またぎの基準月ズレ（潜在バグ）を修正**（各月を年差×12ヶ月でシフト）。
- **v1.3.6** 削減レポで基準年/対象年の未取込を明示する警告を追加。
- **v1.3.5** 削減レポの基準年欄を自動化（後に v1.3.7 で指定復活）。
- **v1.3.4** 削減レポに**インポート用テンプレDL**を追加＋**シミュレーション形式でも取込可**（「月」列の完全一致フォールバック）。
- **v1.3.2 / v1.3.3** サイドバー入力欄の**カーソル（キャレット）表示**を改善（濃紺背景でも見えるよう調整、文字色は白のまま）。
- **v1.3.1** 概算の初期費用に**ハードウェア費（固定＋台数比例）**を追加（機材リスト空でも計上）。
- **v1.3.0** **概算モード**を新設。機材情報が無い案件で、検針票から台数を推定し、初期費用も標準単価×台数で概算（削減式は無改変）。入力画面にトグル、管理画面に概算マスタを追加。

## v1.0.x〜v1.2.x（提案書PPTXの全面リデザイン・分析方法資料）
- 版方針を明文化（資料の非機能改修は軽微パッチ）。
- 提案書PPTXを全面リデザイン：
  - 表紙の配置調整（御中/タイトル/サブタイトル/注釈）
  - P.2 収益サマリーをKPIカード画像化・単位フォント縮小
  - P.4 制御可否（要約バー＋制御可否リスト画像、100件超は続きスライドを自動分割、1枚目は控えめ・続きは全面）
  - P.5 使用量＋デマンドのグラフ画像化＋シナリオ表
  - P.6 10年収支（NotebookLM風：制御%別バー＋累積収支折線＋差分表）
  - P.3 分析方法（支給の3カード image_sort2 ＋「採用した分析手法」7手法グリッドに採択赤枠）
  - P.7 環境価値（ESG）を3アイコンカード＋バナー（支給アイコン）
  - P.8 アドオン工法（空調画像＋施工チェックリスト）
  - P.9 導入スケジュール（支給画像）
  - 旧P.7（財務価値換算）を削除、全ページの左右幅を統一
  - 概算注釈は「算出した数値を掲載するページ」のみに付与

## v1.0.0
- 起点（提案書・見積・削減レポ・請求書・管理画面を備えたベース版）。

---
（注）v1.0.x〜v1.2.x はPPTXリデザインを短期間に多数のパッチで実施したため、上記は主な内容の要約です。以降（v1.3.0〜）は機能単位で記録しています。
